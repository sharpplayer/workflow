version: "3.9"
services:
  angular-app:
    build: .
    ports:
      - "4200:4200"
    volumes:
      - .:/app:cached
      - angular_node_modules:/app/node_modules
    stdin_open: true
    tty: true

volumes:
  angular_node_modules: